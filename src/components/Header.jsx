
const Header = ( {title} ) => {

    return (
        <div className="flex items-center justify-center pt-6 text-xl sm:text-2xl font-sx sm:font-bold">
            {title}
        </div>
    );

}

export default Header;
